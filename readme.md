My implementation of the game 2048. Built with React and Tailwind.

## Install

To install the dependencies for the application, run:

```yarn install```

## Run

To run the game in your browser, run:

```yarn start```
